## if / elif / else
Used to execute code based on conditions.

## for loop
Best for iterating over collections.

## while loop
Repeats until a condition becomes false.

## Functions
Reusable blocks of code that can accept inputs and return outputs.

## return
Ends the function and sends a value back to the caller.

## Type hints
Improve readability and enable better tooling support.

## Docstrings
Explain what a function does, its parameters, and its return value.

### making src.functions importable
```python
PYTHONPATH=. pytest tests/test_functions.py
````